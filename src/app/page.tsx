export default function AuthRedirect() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center bg-white">
      <p>TODO: navigate to Welcome or Main depending on if user is authenticated or not.</p>
    </main>
  );
}
